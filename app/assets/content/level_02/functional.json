{
  "id": "java.functional",
  "levelId": 2,
  "title": "Functional Programming in Java",
  "xp": 40,
  "estimatedMinutes": 30,
  "objectives": [
    "Understand functional programming through a real-life analogy",
    "Apply pure functions, immutability, and higher-order functions",
    "Use Optional properly — no more null checks everywhere",
    "Compose behavior via function chaining",
    "Answer functional-programming interview questions confidently"
  ],
  "sections": [
    { "type": "callout", "variant": "analogy", "markdown": "**Imagine two ways to make a smoothie.**\n\n- **Imperative:** Grab the blender. Add banana. Turn on. Add milk. Off. Add sugar. On. Pour. Every step mutates the shared state (the blender). To repeat the recipe, you must start clean or the last flavor lingers.\n- **Functional:** Each step is a pure function. `add(bananaToEmptyBlender)` → `blenderWithBanana`. `add(bananaBlender, milk)` → `newBlender`. The old blender is untouched — you can rewind and reuse it. Nothing mutates. Nothing has side effects. You could parallelize freely because there's no shared state.\n\n**Functional programming is a style, not a language.** Java has been functional-ready since Java 8 (lambdas + streams + Optional). Adopting the style makes code more predictable, more testable, and more thread-safe." },
    { "type": "callout", "variant": "definition", "markdown": "**Functional programming (FP)** is a style of programming that treats computation as evaluation of **pure functions**, favors **immutability**, and uses **higher-order functions** (functions that take or return other functions). Java 8+ gives you the tools; you provide the discipline." },
    { "type": "theory", "markdown": "## The four core ideas" },
    { "type": "table", "headers": ["Idea", "Meaning", "Example"], "rows": [
      ["Pure function", "Same input → same output. No side effects.", "int square(int x) { return x*x; }"],
      ["Immutability", "Data doesn't mutate — you create new values", "record Point(int x, int y) — no setters"],
      ["Higher-order function", "Function that takes or returns a function", "Comparator.comparing(User::name)"],
      ["Declarative style", "Say WHAT, not HOW", "stream().filter().map() vs for/if loops"]
    ]},
    { "type": "theory", "markdown": "## Pure functions — what they are and aren't" },
    { "type": "code", "language": "java", "code": "// PURE — same input, same output, no observable side effect\nint doubled(int x) { return x * 2; }\nString shout(String s) { return s.toUpperCase() + \"!\"; }\n\n// IMPURE — depends on shared state\nprivate int seed = 0;\nint nextId() { return ++seed; }              // different result each call\n\n// IMPURE — has a side effect\nvoid saveOrder(Order o) { db.insert(o); }   // touches the database\n\n// IMPURE — reads external state\nInstant now() { return Instant.now(); }     // clock changes\n\n// Real apps have BOTH. The trick: push impurity to the edges.\n// Keep the middle pure. Business logic that's pure = easy to test." },
    { "type": "callout", "variant": "interview", "markdown": "**The senior insight: not every function can be pure — I/O and time have to happen somewhere. The pattern is 'functional core, imperative shell.' The center is pure functions on immutable data (testable, parallelizable). The outer shell handles I/O and calls the core. Result: 90% of your code is easily tested; 10% is where the messy world touches the app." },
    { "type": "theory", "markdown": "## Optional — the null killer\n\nBefore Optional (Java 8+), 'the field might be missing' was expressed with `null` — leading to `NullPointerException` everywhere. `Optional<T>` makes 'might be absent' a real type, so the compiler forces you to handle it." },
    { "type": "code", "language": "java", "code": "// BAD — null-checks pyramid\nUser u = repo.findById(id);\nif (u != null) {\n  Address a = u.getAddress();\n  if (a != null) {\n    String c = a.getCity();\n    if (c != null) {\n      System.out.println(c);\n    }\n  }\n}\n\n// GOOD — functional Optional chain\nrepo.findById(id)              // Optional<User>\n    .map(User::getAddress)     // Optional<Address>  — auto-flattens null\n    .map(Address::getCity)     // Optional<String>\n    .ifPresent(System.out::println);\n\n// With a fallback\nString city = repo.findById(id)\n    .map(User::getAddress)\n    .map(Address::getCity)\n    .orElse(\"Unknown\");" },
    { "type": "callout", "variant": "mistake", "markdown": "**Optional pitfalls to avoid:**\n\n- **Never use `Optional` as a field type or method parameter.** It's designed for **return types** — 'this method might not return a value.' Fields should be nullable or use the Null Object pattern.\n- **Don't call `.get()` without checking `.isPresent()`.** That's just `NullPointerException` with extra ceremony. Use `.orElse`, `.orElseGet`, `.map`, `.ifPresent`.\n- **`Optional<List<T>>` is usually wrong.** An empty list already says 'no items.' Use `List<T>` and return `Collections.emptyList()`." },
    { "type": "theory", "markdown": "## Higher-order functions — the FP superpower\n\nA function that takes another function or returns one. Java's whole `java.util.function` package exists for this." },
    { "type": "code", "language": "java", "code": "// A method that returns a function — currying / partial application\nFunction<Integer, Integer> multiplyBy(int factor) {\n  return x -> x * factor;\n}\nFunction<Integer, Integer> triple = multiplyBy(3);\ntriple.apply(10);   // 30\n\n// A method that takes a function\n<T, R> List<R> mapAll(List<T> in, Function<T, R> fn) {\n  return in.stream().map(fn).toList();\n}\nmapAll(List.of(\"a\", \"bb\", \"ccc\"), String::length);   // [1, 2, 3]" },
    { "type": "theory", "markdown": "## A complete sample program — a small pipeline in functional style" },
    { "type": "code", "language": "java", "code": "// File: FunctionalDemo.java\n// Run: javac FunctionalDemo.java && java FunctionalDemo\nimport java.util.*;\nimport java.util.function.*;\nimport java.util.stream.*;\n\nrecord Product(String name, String category, double price, int stock) { }\n\npublic class FunctionalDemo {\n  // Pure predicate\n  static final Predicate<Product> inStock = p -> p.stock() > 0;\n  static Predicate<Product> priceUnder(double max) {\n    return p -> p.price() < max;\n  }\n\n  // Pure transformation\n  static Function<Product, String> label = p -> p.name() + \" ($\" + p.price() + \")\";\n\n  public static void main(String[] args) {\n    List<Product> catalog = List.of(\n      new Product(\"Chair\",  \"furniture\", 2000, 5),\n      new Product(\"Sofa\",   \"furniture\", 9000, 2),\n      new Product(\"Lamp\",   \"lighting\",   800, 0),\n      new Product(\"Bulb\",   \"lighting\",   200, 12),\n      new Product(\"Table\",  \"furniture\", 4000, 0),\n      new Product(\"Cable\",  \"electric\",   300, 20));\n\n    // 1. Composition — inStock AND priceUnder(1000)\n    List<String> affordable = catalog.stream()\n        .filter(inStock.and(priceUnder(1000)))\n        .map(label)\n        .toList();\n    System.out.println(\"in stock, under 1000: \" + affordable);\n\n    // 2. Optional — first product matching a predicate\n    Optional<Product> firstFurniture = catalog.stream()\n        .filter(p -> p.category().equals(\"furniture\") && p.stock() > 0)\n        .findFirst();\n    firstFurniture\n        .map(label)\n        .ifPresentOrElse(\n          s -> System.out.println(\"first furniture: \" + s),\n          () -> System.out.println(\"no furniture in stock\"));\n\n    // 3. Aggregation without mutation\n    double totalValue = catalog.stream()\n        .mapToDouble(p -> p.price() * p.stock())\n        .sum();\n    System.out.println(\"total inventory value: \" + totalValue);\n\n    // 4. Grouping — pure, immutable Map returned\n    Map<String, List<Product>> byCategory = catalog.stream()\n        .collect(Collectors.groupingBy(Product::category));\n    System.out.println(\"categories: \" + byCategory.keySet());\n\n    // 5. Curried price adjuster\n    Function<Double, Function<Product, Double>> priceAfterTax = tax -> p -> p.price() * (1 + tax);\n    Function<Product, Double> afterGst = priceAfterTax.apply(0.18);\n    Function<Product, Double> afterCess = priceAfterTax.apply(0.28);\n    System.out.println(\"chair after GST : \" + afterGst.apply(catalog.get(0)));\n    System.out.println(\"chair after cess: \" + afterCess.apply(catalog.get(0)));\n  }\n}\n\n// Expected output:\n//   in stock, under 1000: [Bulb ($200.0), Cable ($300.0)]\n//   first furniture: Chair ($2000.0)\n//   total inventory value: 20800.0\n//   categories: [furniture, lighting, electric]\n//   chair after GST : 2360.0\n//   chair after cess: 2560.0" },
    { "type": "theory", "markdown": "## Common beginner mistakes" },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 1: Calling `.get()` on Optional without checking.**\n\n```java\nString city = repo.findById(id).get().getAddress().getCity();   // BOOM if empty\n```\n\nJust go through `.map` and `.orElse` — the whole point of Optional." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 2: Using Optional for fields or parameters.**\n\n`class User { Optional<Address> address; }` is a design smell — makes the field mutable and eats memory. Use a nullable field or a Null Object; reserve Optional for return types." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 3: Mutating shared state inside stream operations.**\n\n```java\nList<Integer> results = new ArrayList<>();\nnumbers.parallelStream().forEach(n -> results.add(n * 2));   // RACE\n```\n\nFunctional style says: build new immutable collections; don't mutate shared state. Use `.collect(toList())`." },
    { "type": "theory", "markdown": "## Best practices" },
    { "type": "callout", "variant": "best", "markdown": "**1. Prefer immutable value types.** `record`, `List.copyOf`, `Map.copyOf` — safer defaults." },
    { "type": "callout", "variant": "best", "markdown": "**2. Push side effects to the edges of your code.** Pure business logic in the middle; I/O at the boundaries." },
    { "type": "callout", "variant": "best", "markdown": "**3. Use `Optional` only as a return type.** Never fields, never parameters, never collections." },
    { "type": "callout", "variant": "best", "markdown": "**4. Compose small functions to build big behavior.** `filter.and(predicate)`, `function.andThen(other)`." },
    { "type": "theory", "markdown": "## Practice programs" },
    { "type": "callout", "variant": "note", "markdown": "**Practice 1 (Easy) — Rewrite a null-check chain.** Turn a 4-level if-not-null cascade into a single `Optional.map(...).orElse(...)`.\n\n**Why:** Optional clicks when you do the refactor once." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 2 (Medium) — Pure pipeline.** Transform a list of orders into a Map<Customer, totalSpent> without any mutable state outside collectors.\n\n**Why:** Practice thinking in immutable transformations." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 3 (Hard) — Build a Result<T, E> type.** Similar to Rust's or Kotlin's Result — success or failure — with `map`, `flatMap`, `orElse`. Use it for a login flow with typed errors.\n\n**Why:** Alternative to checked exceptions; growing in popularity for functional Java." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 4 (Expert) — Memoization.** Write `memoize(Function<K, V>)` that caches results. Use `ConcurrentHashMap.computeIfAbsent`.\n\n**Why:** Classic FP pattern; useful for pure expensive functions." },
    { "type": "theory", "markdown": "## Interview Q&A — with full answers you can quote" },
    { "type": "callout", "variant": "interview", "markdown": "**Q1: What is a pure function, and why does it matter?**\n\n**A:** A pure function has two properties: **same input always yields the same output** (deterministic), and **no side effects** (doesn't read or modify anything outside its parameters — no I/O, no shared state, no clock). `int square(int x) { return x*x; }` is pure. `int nextId() { return ++seed; }` is not — it reads and mutates shared state. **Why it matters:** pure functions are trivially testable (just check output for input), safely parallelizable (no shared state = no races), cacheable (same input always same output = memoize freely), and easy to reason about. Real apps mix pure and impure code — the pattern 'functional core, imperative shell' keeps most of your business logic pure and pushes I/O to the edges." },
    { "type": "callout", "variant": "interview", "markdown": "**Q2: How does `Optional` improve on `null`?**\n\n**A:** `null` says nothing — is it 'missing?' 'not applicable?' 'a bug?' The type system can't tell. `Optional<T>` makes 'might be absent' a real type. Methods that might not return a value declare `Optional<T>` explicitly; callers cannot pretend the possibility doesn't exist. And Optional offers a functional API — `.map`, `.flatMap`, `.orElse`, `.orElseGet`, `.ifPresent`, `.filter` — so you compose transformations without nested null checks. Rule: Optional is a *return-type* convention. Don't use it for fields (memory + mutability overhead) or parameters (callers can always pass Optional.of() or Optional.empty()). It's a communication tool for 'this operation might not find what you asked for.'" },
    { "type": "callout", "variant": "interview", "markdown": "**Q3: What is a higher-order function?**\n\n**A:** A function that takes another function as a parameter or returns one. Every Stream operation is higher-order — `filter(Predicate)`, `map(Function)`, `reduce(BinaryOperator)`. `Comparator.comparing(User::name)` is higher-order — it accepts a key extractor function. In Java, higher-order functions became practical with lambdas in Java 8. You can also write your own: `Function<Integer, Integer> multiplyBy(int f)` returns a function. Composition — `f.andThen(g)`, `predicate.and(other)` — is higher-order. This is the primary way you build behavior in FP: assemble small functions into bigger ones." },
    { "type": "callout", "variant": "interview", "markdown": "**Q4: What are the benefits of immutability?**\n\n**A:** **Thread safety** — an immutable object can't be corrupted by concurrent access; no locks needed. **Predictability** — no one is silently mutating the object you're holding; behavior is the same as when you got it. **Cacheable / memoizable** — a hash key or a cached result stays valid because inputs don't change. **Easier debugging** — you can pin down where a value came from; it doesn't drift. **Sharing without defensive copies** — you don't need to `defensiveCopy(list)` before returning it. The costs: sometimes performance (allocation for each 'change') and sometimes verbose (builders for complex types). Java 16's `record` and libraries like Immutables + Lombok's `@Value` make immutable classes trivial to write." },
    { "type": "callout", "variant": "interview", "markdown": "**Q5: What is imperative vs declarative style?**\n\n**A:** **Imperative** — tell the computer *how*. Loops, conditionals, mutation: `for (int i=0; i<list.size(); i++) if (list.get(i).startsWith(\"a\")) result.add(list.get(i));`. **Declarative** — tell the computer *what*. `list.stream().filter(s -> s.startsWith(\"a\")).toList()`. Declarative code is shorter, less error-prone (no off-by-one indexes, no forgotten increment), and more parallelizable (the runtime chooses how). But it can be less flexible — some algorithms are naturally imperative. Modern Java lets you mix: use streams / Optional / records for data pipelines and business logic; drop to imperative when you need fine control. The trend is toward declarative — cleaner code with the same or better performance." },
    { "type": "callout", "variant": "interview", "markdown": "**Q6: What is 'functional core, imperative shell'?**\n\n**A:** An architectural pattern where the center of your application is pure functions on immutable data (deterministic, side-effect-free, easily testable), and the outer 'shell' handles I/O and mutable state (database, HTTP, filesystem). The shell reads inputs, calls into the pure core, gets an immutable output, and writes results. Because the core is pure, unit tests just check function inputs and outputs — no mocks, no setup, no databases. Because the shell is thin, integration tests target only the boundary. Result: 90% of your code is easy to test; only 10% needs mocks. This is one of the most impactful architectural insights of the last decade — and it works in any language, but Java 8+ made it practical." },
    { "type": "callout", "variant": "interview", "markdown": "**Q7: What is currying, and does Java support it?**\n\n**A:** Currying is the technique of transforming a function that takes multiple arguments into a chain of functions each taking one argument. `add(2, 3)` becomes `add(2)(3)` — where `add(2)` returns a function that takes a single number and adds 2 to it. Java doesn't have currying as a language feature, but you can express it manually: `Function<Integer, Function<Integer, Integer>> add = a -> b -> a + b;`. Then `add.apply(2).apply(3)` returns 5. Or store a partial: `Function<Integer, Integer> add2 = add.apply(2);` — a function that adds 2 to anything. This is called **partial application** and it's a form of code reuse. Common in truly functional languages (Haskell, F#, Scala); useful sometimes in Java for reusing configured logic." },
    { "type": "theory", "markdown": "## Real-world examples\n\n- **Every `Optional` usage in Spring Data** (`repo.findById()`) — you're forced to handle 'not found' explicitly.\n- **The whole Streams API** is functional programming — declarative pipelines over immutable data.\n- **Records + sealed classes + pattern matching** turned Java into a solid FP language for domain modeling.\n- **Result / Either types** (from Vavr, Arrow, Micronaut) — functional alternative to exceptions for expected failures.\n- **Reactive libraries** (RxJava, Reactor) are FP taken to their limit — every operation is a function transforming an event stream." },
    { "type": "theory", "markdown": "## Advanced notes\n\n- **Persistent data structures** — Vavr's `List`, `Map`, `Set` are immutable but share memory via structural sharing. Fast updates without copies.\n- **Monads** — `Optional`, `Stream`, `CompletableFuture` are all monads (has `map` + `flatMap`). Understanding the pattern unlocks FP.\n- **Lazy evaluation** — streams are lazy; Vavr's Stream is even lazier (finite streams from infinite sources).\n- **Effect systems** (Cats Effect, ZIO in Scala) — model I/O and side effects as pure values. Advanced but powerful.\n- **Kotlin's stdlib** heavily leans functional — `let`, `apply`, `also`, extension functions on Optional-like `Result`. If you like FP in Java, Kotlin is a nice next step.\n- **Referential transparency** — the property that an expression can be replaced with its value without changing behavior. True of pure functions; false of impure ones. Enables mathematical reasoning about code." }
  ],
  "flashcards": [
    { "front": "Pure function?", "back": "Same input → same output. No side effects. Testable + parallelizable + cacheable." },
    { "front": "When to use Optional?", "back": "As a return type — 'this operation might not find a value.' Never fields, never parameters." },
    { "front": "Higher-order function?", "back": "A function that takes or returns another function. Filter, map, comparing — all higher-order." },
    { "front": "Functional core, imperative shell?", "back": "Pure logic in the middle (easy to test); I/O and mutation at the edges." },
    { "front": "Currying?", "back": "Transform a multi-arg function into a chain of single-arg functions. Enables partial application." }
  ],
  "quiz": [
    { "q": "A pure function has…", "options": ["Side effects", "Global state access", "Same output for same input + no side effects", "Nondeterministic behavior"], "answer": 2, "explain": "Deterministic and side-effect-free." },
    { "q": "Optional is best used…", "options": ["As a field type", "As a method parameter", "As a return type", "As a collection element"], "answer": 2, "explain": "Signal that a method might not return a value." },
    { "q": "Calling `.get()` on an empty Optional…", "options": ["Returns null", "Throws NoSuchElementException", "Returns 0", "Blocks"], "answer": 1, "explain": "Which defeats the purpose. Use .map/.orElse instead." },
    { "q": "Immutability primarily helps with…", "options": ["Faster runtime", "Thread safety + easier reasoning", "Smaller code", "Fewer imports"], "answer": 1, "explain": "No shared mutable state = no races; predictable behavior." },
    { "q": "'Functional core, imperative shell' means…", "options": ["Wrap everything in functions", "Pure logic inside; I/O and mutation at the edges", "Only use lambdas", "Ban null"], "answer": 1, "explain": "Testable center, thin I/O shell." }
  ],
  "challenges": [
    { "level": "Easy", "text": "Rewrite a null-check chain (`if (x != null && x.getY() != null...)`) using `Optional.map(...).orElse(...)`." },
    { "level": "Easy", "text": "Write a pure function `String slug(String title)` that returns a URL-safe slug. No shared state." },
    { "level": "Medium", "text": "Transform a list of orders into a `Map<Customer, Double>` (total spent) using only stream + collectors — no external mutation." },
    { "level": "Medium", "text": "Build a `Function<Integer, Integer>` that multiplies by a given factor, using currying: `multiplyBy(3).apply(10) == 30`." },
    { "level": "Hard", "text": "Build `Result<T, E>` (success or failure) with map/flatMap/orElse. Use it for a login: `Result<User, LoginError> login(...)`." },
    { "level": "Hard", "text": "Implement memoization: `memoize(Function<K, V>)` caches results using ConcurrentHashMap.computeIfAbsent." },
    { "level": "Expert", "text": "Refactor a real service (e.g., pricing engine) to functional-core, imperative-shell. Prove the core is 100% testable without mocks." }
  ],
  "interviewQuestions": [
    "What is a pure function, and why does it matter?",
    "How does `Optional` improve on `null`?",
    "What is a higher-order function?",
    "What are the benefits of immutability?",
    "What is imperative vs declarative style?",
    "What is 'functional core, imperative shell'?",
    "What is currying, and does Java support it?",
    "What is referential transparency?"
  ],
  "references": [
    { "label": "Optional — Baeldung", "url": "https://www.baeldung.com/java-optional" },
    { "label": "Functional Programming in Java (Venkat Subramaniam)", "url": "https://pragprog.com/titles/vsjava8/functional-programming-in-java/" }
  ]
}
