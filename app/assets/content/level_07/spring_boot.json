{
  "id": "be.spring_boot",
  "levelId": 7,
  "title": "Spring Boot",
  "xp": 50,
  "estimatedMinutes": 45,
  "objectives": [
    "Understand Spring Boot via a real-life analogy",
    "Know the core building blocks: Beans, IoC, `@RestController`, `@Service`, `@Repository`",
    "Configure profiles, application properties, and DI properly",
    "Wire a REST endpoint to JPA with transactions",
    "Answer Spring Boot interview questions confidently"
  ],
  "sections": [
    { "type": "callout", "variant": "analogy", "markdown": "**Think of Spring Boot as a fully-furnished apartment vs an empty warehouse.**\n\n- **Plain Spring** was an empty warehouse — powerful, flexible, but you had to install every electrical outlet, hang every light, wire every plumbing yourself. XML configuration was thousands of lines. 'Hello world' took an afternoon.\n- **Spring Boot** is a fully-furnished apartment — the kitchen is stocked, the lights work, the internet's on. You walk in and start LIVING. To add a REST endpoint you write ONE class. To add a database you list ONE dependency. Sensible defaults everywhere; overrides where you need them.\n\n**Concretely:** Spring Boot is Spring + convention-over-configuration + an embedded server + starter dependencies. `mvn spring-boot:run` and your app is live on port 8080. Add `spring-boot-starter-web` → you get Tomcat + JSON + validation. Add `spring-boot-starter-data-jpa` → you get connection pooling + Hibernate + transaction management. Zero XML, minimal config.\n\n**Why it dominates:** every enterprise Java backend written in the last 10 years is Spring Boot. Every microservice on JVM. Every Kotlin backend. It's the JVM's answer to Ruby on Rails or Django — but arguably more powerful, less opinionated, better for large teams. The trade-off: it's HUGE. There are 200+ Spring projects; the docs are a book; runtime magic (`@Autowired`, `@Transactional`) can confuse. Learn the CORE (IoC, Beans, controllers, JPA, config) — that's 90% of daily use." },
    { "type": "callout", "variant": "definition", "markdown": "**Spring Boot** is a framework that makes it easy to build production-ready Spring applications with minimal configuration. Built on **Spring Framework** (IoC container + DI + AOP), it adds: **starter dependencies** (`spring-boot-starter-web` pulls in a working web stack), **auto-configuration** (Spring figures out what beans to create based on the classpath), **embedded server** (Tomcat/Jetty runs inside your JAR — no external app server), and **`application.yaml`/`.properties`** configuration. Standard for enterprise Java backends since ~2014." },
    { "type": "theory", "markdown": "## The core building blocks" },
    { "type": "table", "headers": ["Annotation", "What it does", "When"], "rows": [
      ["**`@SpringBootApplication`**", "Marks the main class; enables auto-config, component-scan, config properties", "Application entry point"],
      ["**`@RestController`**", "Marks a class as a REST controller (`@Controller` + `@ResponseBody`)", "HTTP endpoints"],
      ["**`@Service`**", "Marks a business-logic bean", "Application services"],
      ["**`@Repository`**", "Marks a data-access bean; adds exception translation", "DAOs / JPA repositories"],
      ["**`@Component`**", "Generic bean; base of `@Service`, `@Repository`, `@Controller`", "Any bean not fitting the others"],
      ["**`@Autowired`**", "Inject a bean by type (also works via constructor)", "Dependencies"],
      ["**`@Configuration`** + `@Bean`", "Define beans programmatically", "Third-party integrations"],
      ["**`@Value`**", "Inject a property from `application.yaml`", "Config values"],
      ["**`@ConfigurationProperties`**", "Bind a whole config section into a typed class", "Structured config"],
      ["**`@Transactional`**", "Wrap method in a DB transaction", "Service methods with DB writes"]
    ]},
    { "type": "theory", "markdown": "## Sample program — a complete REST + JPA slice" },
    { "type": "code", "language": "java", "code": "// build.gradle\n// dependencies {\n//   implementation 'org.springframework.boot:spring-boot-starter-web'\n//   implementation 'org.springframework.boot:spring-boot-starter-data-jpa'\n//   implementation 'org.springframework.boot:spring-boot-starter-validation'\n//   runtimeOnly    'org.postgresql:postgresql'\n// }\n\n// src/main/java/com/example/user/Application.java\n@SpringBootApplication\npublic class Application {\n  public static void main(String[] args) { SpringApplication.run(Application.class, args); }\n}\n\n// application.yaml\n// server:\n//   port: 8080\n// spring:\n//   datasource:\n//     url: jdbc:postgresql://localhost:5432/myapp\n//     username: myapp\n//     password: ${DB_PASS}\n//   jpa:\n//     hibernate:\n//       ddl-auto: validate\n//     show-sql: false\n\n// Entity\n@Entity\n@Table(name = \"users\")\npublic class User {\n  @Id @GeneratedValue(strategy = GenerationType.IDENTITY)\n  private Long id;\n  @Column(nullable = false, unique = true) private String email;\n  @Column(nullable = false) private String name;\n  private Instant createdAt = Instant.now();\n  // getters / setters / no-arg ctor omitted\n}\n\n// Repository — Spring Data auto-implements at runtime.\npublic interface UserRepository extends JpaRepository<User, Long> {\n  Optional<User> findByEmail(String email);\n  boolean existsByEmail(String email);\n}\n\n// Service — business logic; transactional.\n@Service\npublic class UserService {\n  private final UserRepository users;\n  public UserService(UserRepository users) { this.users = users; }\n\n  public User getById(Long id) {\n    return users.findById(id)\n        .orElseThrow(() -> new NotFoundException(\"User \" + id));\n  }\n\n  @Transactional\n  public User create(CreateUserRequest req) {\n    if (users.existsByEmail(req.email())) {\n      throw new ConflictException(\"Email already exists\");\n    }\n    var user = new User();\n    user.setEmail(req.email());\n    user.setName(req.name());\n    return users.save(user);\n  }\n\n  @Transactional\n  public void delete(Long id) {\n    users.deleteById(id);\n  }\n}\n\n// Controller — thin. Just wire HTTP to the service.\n@RestController\n@RequestMapping(\"/api/v1/users\")\npublic class UserController {\n  private final UserService svc;\n  public UserController(UserService svc) { this.svc = svc; }\n\n  @GetMapping(\"/{id}\")\n  public UserDto get(@PathVariable Long id) {\n    return UserDto.from(svc.getById(id));\n  }\n\n  @PostMapping\n  public ResponseEntity<UserDto> create(@Valid @RequestBody CreateUserRequest req) {\n    var u = svc.create(req);\n    return ResponseEntity.created(URI.create(\"/api/v1/users/\" + u.getId()))\n        .body(UserDto.from(u));\n  }\n\n  @DeleteMapping(\"/{id}\")\n  public ResponseEntity<Void> delete(@PathVariable Long id) {\n    svc.delete(id);\n    return ResponseEntity.noContent().build();\n  }\n}\n\n// DTO + validated request record\npublic record CreateUserRequest(\n    @NotBlank String name,\n    @Email @NotBlank String email\n) {}\n\npublic record UserDto(Long id, String name, String email, Instant createdAt) {\n  public static UserDto from(User u) {\n    return new UserDto(u.getId(), u.getName(), u.getEmail(), u.getCreatedAt());\n  }\n}\n\n// Global error handler — maps exceptions to HTTP responses.\n@RestControllerAdvice\npublic class ErrorHandler {\n  @ExceptionHandler(NotFoundException.class)\n  public ResponseEntity<?> notFound(NotFoundException e) {\n    return ResponseEntity.status(404).body(Map.of(\"code\", \"NOT_FOUND\", \"message\", e.getMessage()));\n  }\n  @ExceptionHandler(ConflictException.class)\n  public ResponseEntity<?> conflict(ConflictException e) {\n    return ResponseEntity.status(409).body(Map.of(\"code\", \"CONFLICT\", \"message\", e.getMessage()));\n  }\n  @ExceptionHandler(MethodArgumentNotValidException.class)\n  public ResponseEntity<?> validation(MethodArgumentNotValidException e) {\n    var errors = e.getBindingResult().getFieldErrors().stream()\n        .collect(Collectors.toMap(FieldError::getField, DefaultMessageSourceResolvable::getDefaultMessage));\n    return ResponseEntity.badRequest().body(Map.of(\"code\", \"VALIDATION\", \"fields\", errors));\n  }\n}\n\n// Run:  ./gradlew bootRun     ← app is live on :8080." },
    { "type": "theory", "markdown": "## Common mistakes" },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 1: Field injection with `@Autowired` on fields.** `@Autowired private UserService svc;` — mutable, hides deps, hard to test. Use CONSTRUCTOR injection (Spring 4.3+ makes `@Autowired` optional on single constructors)." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 2: Business logic in controllers.** Controllers should be THIN — parse request, call service, return response. Business rules live in `@Service` beans, testable in isolation." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 3: `@Transactional` on controllers.** Transactions belong on Service methods. Controllers have no business managing DB lifetimes." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 4: Exposing JPA entities as JSON.** `return userRepo.findById(id);` — leaks internal fields, eagerly fetches lazy relations, breaks encapsulation. Always convert to a DTO." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 5: Ignoring profiles.** `dev`, `test`, `prod` profiles let you swap config cleanly. `spring.profiles.active=prod` picks `application-prod.yaml` overrides." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 6: `ddl-auto: create-drop` in production.** Wipes the DB on every restart. Use `validate` in prod; migrations via Flyway or Liquibase." },
    { "type": "theory", "markdown": "## Best practices" },
    { "type": "callout", "variant": "best", "markdown": "**1. Constructor injection.** Explicit, testable, immutable." },
    { "type": "callout", "variant": "best", "markdown": "**2. Thin controllers, fat services.** Controllers translate HTTP; services own logic." },
    { "type": "callout", "variant": "best", "markdown": "**3. DTOs at the boundary.** Never return JPA entities directly." },
    { "type": "callout", "variant": "best", "markdown": "**4. `@Transactional` on services.** Not repositories, not controllers." },
    { "type": "callout", "variant": "best", "markdown": "**5. Profiles for environments.** `dev`, `test`, `prod` — each with its own `application-*.yaml`." },
    { "type": "callout", "variant": "best", "markdown": "**6. Flyway/Liquibase for DB migrations.** Version-controlled SQL; `ddl-auto: validate` in prod." },
    { "type": "callout", "variant": "best", "markdown": "**7. Test with `@SpringBootTest` + Testcontainers.** Real Postgres in tests; no H2 fakery." },
    { "type": "callout", "variant": "best", "markdown": "**8. Actuator for ops.** `/actuator/health`, `/actuator/metrics`, `/actuator/info` — free monitoring endpoints." },
    { "type": "theory", "markdown": "## Practice programs" },
    { "type": "callout", "variant": "note", "markdown": "**Practice 1 (Easy) — Hello World endpoint.** `@GetMapping(\"/hello\")` returning `\"Hello, world!\"`. Run with `bootRun`." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 2 (Medium) — User CRUD.** Build the sample. Test with curl / Postman." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 3 (Medium) — Add validation.** `@NotBlank`, `@Email` on request records; verify 400 with field errors on invalid input." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 4 (Hard) — Profiles + config.** `application-dev.yaml` uses H2; `application-prod.yaml` uses Postgres. `spring.profiles.active=dev` selects." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 5 (Hard) — Flyway migrations.** `V1__create_users.sql` + `V2__add_deleted_at.sql`. Deploy without downtime." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 6 (Expert) — Testcontainers.** Real Postgres in `@SpringBootTest`; each test spins up a fresh container." },
    { "type": "theory", "markdown": "## Interview Q&A — with full answers you can quote" },
    { "type": "callout", "variant": "interview", "markdown": "**Q1: What is Spring Boot and how does it differ from Spring?**\n\n**A:** Spring is the underlying framework — IoC container, dependency injection, AOP, transaction management, MVC. Powerful but historically required massive XML/Java configuration. Spring Boot is a wrapper around Spring that provides: **(1) starter dependencies** (e.g., `spring-boot-starter-web` pulls in Tomcat, Jackson, validation), **(2) auto-configuration** (Spring examines the classpath and creates default beans), **(3) embedded server** (Tomcat/Jetty run INSIDE your JAR — no external app server), **(4) `application.yaml/properties`** conventions, **(5) production features** (Actuator for health/metrics). Result: 'hello world' in 3 files instead of 30. Every modern Java backend is Spring Boot." },
    { "type": "callout", "variant": "interview", "markdown": "**Q2: What is Inversion of Control and Dependency Injection?**\n\n**A:** **IoC (Inversion of Control)** — instead of your code CREATING its dependencies (`new UserRepository()`), a container (Spring's `ApplicationContext`) creates them and hands them to you. Control of object creation is inverted from your code to the framework. **DI (Dependency Injection)** — the mechanism: dependencies are PROVIDED (via constructor / setter / field), not fetched. Spring reads your `@Component`/`@Service` classes at startup, builds a graph of beans, and injects each into the ones that need it. **Result:** classes depend on interfaces (testable), one place wires them together, swappable in tests. This is the beating heart of Spring — everything else builds on it. Interview: mention that DI is HOW you achieve IoC." },
    { "type": "callout", "variant": "interview", "markdown": "**Q3: `@Component` vs `@Service` vs `@Repository` vs `@Controller`?**\n\n**A:** All four are variants of `@Component` — they mark classes as Spring-managed beans. Differences are semantic + minor behavior: **`@Component`** — generic bean. **`@Service`** — business logic (semantically distinct; no extra behavior). **`@Repository`** — data-access bean; adds **exception translation** (JPA `PersistenceException` → Spring `DataAccessException`). **`@Controller`** — MVC controller returning views. **`@RestController`** — `@Controller` + `@ResponseBody` — every method returns JSON. **Rule:** use the most specific annotation; Spring can adjust behavior based on it (e.g., transactions, exception translation). Interview: emphasize `@RestController` for REST APIs and `@Repository` for JPA classes." },
    { "type": "callout", "variant": "interview", "markdown": "**Q4: How does Spring Boot's auto-configuration work?**\n\n**A:** Spring Boot scans the classpath and creates default beans based on what it finds. Example: **classpath has `spring-boot-starter-web`?** → auto-create a Tomcat server, register Jackson for JSON, add validators. **classpath has `spring-boot-starter-data-jpa`?** → auto-configure a connection pool (HikariCP), an EntityManagerFactory, transaction manager. Auto-configuration is driven by `@Conditional*` annotations — 'create THIS bean only if THAT class is on the classpath AND no bean of this type exists yet'. Your own `@Bean` methods override auto-configured defaults. **`spring-boot-starter-*` dependencies** are the trigger — each pulls the right stack for one concern. This is why 'add a dep, get a feature' works so well." },
    { "type": "callout", "variant": "interview", "markdown": "**Q5: How do transactions work in Spring?**\n\n**A:** `@Transactional` wraps a method in a database transaction — BEGIN before the method, COMMIT on normal return, ROLLBACK on uncaught runtime exception. Implemented via **AOP** — Spring creates a proxy around your bean; the proxy handles begin/commit/rollback around each `@Transactional` method call. **Gotchas:** **(1) Self-invocation doesn't trigger the proxy** — calling `this.method()` bypasses `@Transactional`. Refactor to a separate bean. **(2) Only `RuntimeException` triggers rollback** by default, NOT checked exceptions — configure with `rollbackFor=Exception.class`. **(3) Propagation** — default is `REQUIRED` (join existing txn if any); `REQUIRES_NEW` starts a fresh one. **(4) Read-only** — mark with `@Transactional(readOnly = true)` for GETs (small performance win)." },
    { "type": "callout", "variant": "interview", "markdown": "**Q6: What are Spring profiles?**\n\n**A:** A way to activate different configuration per environment. `spring.profiles.active=prod` → Spring loads `application-prod.yaml` on top of `application.yaml`. `dev`, `test`, `prod` are conventions. Also gates beans: `@Profile(\"prod\")` on a bean makes it exist only in that profile. **Use cases:** different DB URLs per env, different logging, dev-only mocks, prod-only monitoring beans. **Setting the profile:** `-Dspring.profiles.active=prod`, `SPRING_PROFILES_ACTIVE=prod` env var, or `spring.profiles.active` in properties. **Rule:** never hard-code environment values; profiles + env vars = clean separation. Use `dev`, `test`, `prod` at minimum; some teams add `staging`, `local`." },
    { "type": "callout", "variant": "interview", "markdown": "**Q7: How do you test a Spring Boot app?**\n\n**A:** Three levels. **(1) Unit tests** — plain JUnit, no Spring. Inject fakes into services; test business logic in milliseconds. Fast, hermetic. **(2) `@WebMvcTest` / `@DataJpaTest`** — slice tests. Spin up ONLY the controller layer or DB layer. Faster than full context. **(3) `@SpringBootTest`** — full application context. Ideal for integration tests. Combine with **Testcontainers** to run real Postgres/Redis/Kafka in Docker containers per test class. **Best practice:** unit tests for services (95%), slice tests for controllers/repositories (4%), full integration tests for critical paths (1%). Avoid the 'test everything with `@SpringBootTest`' anti-pattern — startup time kills your test suite." },
    { "type": "callout", "variant": "interview", "markdown": "**Q8: What is Spring Actuator and why is it useful?**\n\n**A:** Spring Boot Actuator adds production-ready operational endpoints — `/actuator/health` (is the app alive?), `/actuator/metrics` (memory, GC, HTTP counters), `/actuator/info` (build metadata), `/actuator/prometheus` (for Prometheus scraping), `/actuator/loggers` (change log level at runtime), `/actuator/env` (view current config). All auto-configured — just add `spring-boot-starter-actuator`. **Why:** every prod service needs a `/health` endpoint for K8s liveness/readiness probes. Every prod service needs metrics for observability. Actuator gives you both without writing them. **Security:** endpoints are sensitive by default; expose only `/health` publicly, protect the rest via Spring Security. Actuator alone justifies using Spring Boot over a hand-rolled framework." },
    { "type": "theory", "markdown": "## Real-world usage\n\n- **Every enterprise Java backend** in the last 10 years.\n- **Netflix, Alibaba, PayPal, Airbnb** — all heavy Spring Boot users.\n- **Kotlin backends** — Spring Boot supports Kotlin natively.\n- **Microservices** — the dominant JVM microservice framework.\n- **Reactive stacks** — Spring WebFlux for non-blocking (alternative to Spring MVC).\n- **Cloud-native** — Spring Cloud adds service discovery, config server, circuit breakers.\n- **Alternatives** — Quarkus, Micronaut (faster startup for serverless), Ktor (Kotlin-idiomatic)." },
    { "type": "theory", "markdown": "## Advanced notes\n\n- **`@Async`** + `@EnableAsync` — background execution via a task executor.\n- **`@Scheduled`** — cron-style scheduled tasks.\n- **`ApplicationEventPublisher`** — in-process pub/sub between beans.\n- **`ApplicationRunner` / `CommandLineRunner`** — code that runs at startup.\n- **`WebClient`** (WebFlux) — reactive HTTP client, replaces RestTemplate.\n- **`RestTemplate` / `RestClient`** — synchronous HTTP clients.\n- **Spring WebFlux** — reactive stack (Netty, Mono/Flux); best for high-concurrency I/O.\n- **Spring Security** — auth + authz; complex but powerful. See its own topic.\n- **Spring Cloud** — service discovery (Eureka), config server, circuit breaker (Resilience4j), gateway.\n- **`@ConditionalOnProperty`** — bean creation gated by property value.\n- **`spring-boot-devtools`** — hot reload in dev.\n- **JMX + Actuator** — legacy but still useful.\n- **Spring AOT / native image** — GraalVM native compilation for faster startup." }
  ],
  "flashcards": [
    { "front": "Spring Boot in one line?", "back": "Spring + convention-over-configuration + starter deps + embedded server + auto-configuration." },
    { "front": "Core annotations?", "back": "@RestController, @Service, @Repository, @Component, @Autowired, @Configuration + @Bean, @Transactional." },
    { "front": "IoC + DI?", "back": "IoC = framework creates dependencies. DI = provided via constructor/setter/field. Result: testable, decoupled." },
    { "front": "@Transactional pitfalls?", "back": "Self-invocation bypasses proxy. Only RuntimeException triggers rollback by default. Put on services, not controllers." },
    { "front": "Profiles?", "back": "spring.profiles.active=prod loads application-prod.yaml. Per-env config." },
    { "front": "Actuator?", "back": "Production endpoints: /health, /metrics, /prometheus. Free monitoring." }
  ],
  "quiz": [
    { "q": "Preferred DI style?", "options": ["Field @Autowired", "Constructor injection", "Setter injection", "Static access"], "answer": 1, "explain": "Explicit, testable, immutable." },
    { "q": "@Repository adds?", "options": ["Nothing extra", "JPA exception translation", "Only auto-scan", "Deprecated"], "answer": 1, "explain": "Persistence exceptions → DataAccessException." },
    { "q": "@Transactional on…", "options": ["Controllers", "Service methods", "Repositories", "Configuration classes"], "answer": 1, "explain": "Services own transactions." },
    { "q": "Auto-config trigger?", "options": ["XML", "Classpath + @Conditional annotations", "Reflection only", "Manual"], "answer": 1, "explain": "Starters bring deps; conditions gate beans." },
    { "q": "Actuator provides?", "options": ["Nothing", "Health / metrics / prometheus / info endpoints", "Compilation only", "Frontend"], "answer": 1, "explain": "Production ops out of the box." }
  ],
  "challenges": [
    { "level": "Easy", "text": "Hello endpoint at /hello. bootRun it." },
    { "level": "Easy", "text": "Register a @Bean for a Clock; inject in another bean." },
    { "level": "Medium", "text": "User CRUD with JPA + validation + error handler." },
    { "level": "Medium", "text": "Add dev/prod profiles with different DB URLs." },
    { "level": "Hard", "text": "Flyway migrations V1..V3 with data seed." },
    { "level": "Hard", "text": "@Scheduled task that runs every 5 minutes; use profile to disable in tests." },
    { "level": "Expert", "text": "Testcontainers for Postgres + Kafka in @SpringBootTest." }
  ],
  "interviewQuestions": [
    "What is Spring Boot and how does it differ from Spring?",
    "What is Inversion of Control and Dependency Injection?",
    "@Component vs @Service vs @Repository vs @Controller?",
    "How does Spring Boot's auto-configuration work?",
    "How do transactions work in Spring?",
    "What are Spring profiles?",
    "How do you test a Spring Boot app?",
    "What is Spring Actuator and why is it useful?"
  ],
  "references": [
    { "label": "Spring Boot docs", "url": "https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/" },
    { "label": "Spring guides", "url": "https://spring.io/guides" }
  ]
}
