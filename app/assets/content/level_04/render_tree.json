{
  "id": "flutter.render_tree",
  "levelId": 4,
  "title": "Render Tree",
  "xp": 45,
  "estimatedMinutes": 35,
  "objectives": [
    "Understand the render tree via a real-life analogy",
    "See how constraints flow down and sizes flow up",
    "Distinguish RenderObject from RenderBox from Sliver",
    "Know when to use RepaintBoundary",
    "Answer render-tree interview questions"
  ],
  "sections": [
    { "type": "callout", "variant": "analogy", "markdown": "**Think of a construction site's actual physical structures.**\n\n- Widgets are BLUEPRINTS. Elements are the FOREMEN with clipboards. **RenderObjects are the ACTUAL BUILDINGS** — walls with dimensions, floors with weight, roofs painted a color. When you look at your Flutter app on screen, you're looking at the render tree.\n- Every RenderObject has to answer three questions each frame: **(1) How big am I?** (layout — driven by constraints from parent), **(2) Where are my children?** (positioning), **(3) What do I paint?** (rendering to the canvas).\n- Constraints flow DOWN the tree (a parent says 'you can be at most 300 pixels wide'), sizes flow UP ('OK, I'll be 150'). That's Flutter's layout algorithm — one traversal down for constraints, one traversal up for sizes, done in a single frame.\n- **Repaints are expensive.** If you scroll a big list, every frame the visible cells' RenderObjects have to be re-laid-out and re-painted. Flutter has an escape valve: **`RepaintBoundary`** — 'this subtree paints to its own layer; don't re-paint it unless something inside actually changed.'\n\n**Why this matters:** the render tree is where Flutter meets the GPU. Understanding it is what separates 'I can build widgets' from 'I can debug that mysterious jank on a Samsung A20.'" },
    { "type": "callout", "variant": "definition", "markdown": "**The Render tree** is the tree of `RenderObject`s that actually lay out, paint, and hit-test on screen. Each RenderObject implements `performLayout()`, `paint()`, and `hitTest()`. Constraints flow DOWN (from parent), sizes flow UP (from children); this two-pass algorithm resolves layout in a single frame. **`RenderBox`** is the common 2D-cartesian subclass; **`RenderSliver`** is the scrollable-axis variant." },
    { "type": "theory", "markdown": "## The layout algorithm — constraints down, sizes up" },
    { "type": "code", "language": "text", "code": "PARENT LAYOUT PASS (top-down):\n  parent.performLayout()\n    for each child:\n      constraints = compute constraints for THIS child\n                    (e.g. 'you can be 0-300px wide, 0-inf tall')\n      child.layout(constraints, parentUsesSize: true)\n\nCHILD LAYOUT PASS (bottom-up, called by parent.layout):\n  child.performLayout()\n    layout its own children the same way\n    set child.size to something within `constraints`\n\nPARENT POSITIONS CHILDREN:\n  now that each child has a size, parent decides where each sits\n  (parentData.offset = ...)\n\nSize.dry / getDryLayout — sometimes children can be measured WITHOUT\nrunning full layout (e.g. IntrinsicWidth wraps this).\n\nKEY RULE:\n  Constraints are IMMUTABLE inputs.\n  A child MUST return a size that satisfies its constraints.\n  A parent should not read child.size without parentUsesSize: true." },
    { "type": "theory", "markdown": "## The three kinds of RenderObject you'll meet" },
    { "type": "table", "headers": ["Class", "Purpose", "Example widgets"], "rows": [
      ["`RenderBox`", "2D box with (width, height) constraints", "Most Flutter UI: Text, Container, Row/Column, Padding"],
      ["`RenderSliver`", "Scrollable-axis layout: measures 'available scroll extent' and returns 'my scroll extent'", "SliverList, SliverGrid, SliverAppBar"],
      ["`RenderObject` (root)", "Base class — abstract", "Rarely subclassed directly"]
    ]},
    { "type": "theory", "markdown": "## Painting — layers, compositing, and RepaintBoundary" },
    { "type": "code", "language": "dart", "code": "// The paint phase walks the render tree, calling paint() on each RenderObject.\n// A RenderObject that has isRepaintBoundary=true (or is wrapped in a\n// RepaintBoundary widget) paints to its OWN compositing LAYER.\n\nRepaintBoundary(\n  child: AnimatedContainer(\n    duration: Duration(milliseconds: 300),\n    color: color,\n    child: /* expensive subtree */,\n  ),\n)\n\n// Effect: when the color changes and only this AnimatedContainer needs to\n// repaint, Flutter re-records its layer WITHOUT touching neighboring layers.\n// The compositor blends the (unchanged) neighbors + (updated) layer on GPU.\n// This is a huge win when you have a big list scrolling next to a static header.\n\n// See it in DevTools: 'Highlight Repaints' — layers turn different colors when\n// they re-record. If layers you didn't expect are flashing, wrap them in a\n// RepaintBoundary." },
    { "type": "theory", "markdown": "## Sample program — write a tiny custom RenderBox" },
    { "type": "code", "language": "dart", "code": "import 'package:flutter/material.dart';\nimport 'package:flutter/rendering.dart';\n\nvoid main() => runApp(MaterialApp(home: Scaffold(\n  appBar: AppBar(title: const Text('Custom RenderBox')),\n  body: const Center(child: PulseBox(color: Colors.deepPurple, size: 200)),\n)));\n\n// A widget backed by a custom RenderObject that paints a filled square.\n// This is the same pattern used by every leaf widget in Flutter (Text, Image,\n// ColoredBox, RawImage) — a RenderObjectWidget wrapping a RenderBox.\nclass PulseBox extends LeafRenderObjectWidget {\n  const PulseBox({super.key, required this.color, required this.size});\n  final Color color;\n  final double size;\n\n  @override\n  RenderObject createRenderObject(BuildContext context) =>\n      _RenderPulseBox(color: color, side: size);\n\n  @override\n  void updateRenderObject(BuildContext context, _RenderPulseBox render) {\n    render\n      ..color = color\n      ..side = size;\n  }\n}\n\nclass _RenderPulseBox extends RenderBox {\n  _RenderPulseBox({required Color color, required double side})\n    : _color = color, _side = side;\n\n  Color _color;\n  double _side;\n  set color(Color v) { if (_color == v) return; _color = v; markNeedsPaint(); }\n  set side(double v) { if (_side == v) return; _side = v; markNeedsLayout(); }\n\n  @override\n  void performLayout() {\n    // Constraints flow DOWN — we must respect them.\n    size = constraints.constrain(Size(_side, _side));\n  }\n\n  @override\n  void paint(PaintingContext ctx, Offset offset) {\n    final rect = offset & size;\n    ctx.canvas.drawRect(rect, Paint()..color = _color);\n  }\n\n  @override\n  bool hitTestSelf(Offset position) => true;   // consume taps\n}\n\n// This is exactly how RenderObject internals work throughout Flutter.\n// Notice the pattern: setters call markNeedsPaint or markNeedsLayout,\n// invalidating the correct pipeline stage. Flutter re-runs only what's dirty." },
    { "type": "theory", "markdown": "## Common beginner mistakes" },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 1: Reading `size` without asking for it.** Parents must pass `parentUsesSize: true` when calling `child.layout(...)` — otherwise Flutter throws in checked mode." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 2: Ignoring constraints.** A child MUST return a size satisfying its constraints. Returning too small/large causes overflow errors or the yellow-and-black striped 'RenderFlex overflowed' banner." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 3: Not calling `markNeedsPaint` / `markNeedsLayout` when props change.** Your setter mutates the field but doesn't invalidate — the frame won't repaint. Every prop setter on a RenderObject must invalidate the right pipeline stage." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 4: Missing `RepaintBoundary` around expensive animated subtrees.** Every neighboring widget re-records its layer on every frame. Wrap the animation in a `RepaintBoundary` so its neighbors are undisturbed." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 5: Doing expensive computation in `paint()`.** `paint` runs every frame the layer needs re-recording. Cache Path objects, avoid allocating in the paint loop. Do the heavy math in `performLayout` or in the widget-level State." },
    { "type": "theory", "markdown": "## Best practices" },
    { "type": "callout", "variant": "best", "markdown": "**1. Wrap expensive animated subtrees in `RepaintBoundary`.** Isolates repaints to that subtree." },
    { "type": "callout", "variant": "best", "markdown": "**2. Enable DevTools' 'Highlight Repaints' during perf work.** See exactly which layers are re-recording each frame." },
    { "type": "callout", "variant": "best", "markdown": "**3. In custom RenderObjects, cache what you can.** Path objects, computed sizes, painters." },
    { "type": "callout", "variant": "best", "markdown": "**4. Prefer high-level widgets to custom RenderObjects.** 99% of UIs never need a custom RenderObject — composition of Row/Column/CustomPaint suffices." },
    { "type": "callout", "variant": "best", "markdown": "**5. Understand constraints (`BoxConstraints`) intuitively.** Whenever you see 'RenderFlex overflowed', 'unbounded constraints', etc., the fix is always in the constraint chain." },
    { "type": "theory", "markdown": "## Practice programs" },
    { "type": "callout", "variant": "note", "markdown": "**Practice 1 (Easy) — Wrap in RepaintBoundary.** Take an app with a running animation next to a big list; wrap the animation in `RepaintBoundary`; enable Highlight Repaints in DevTools." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 2 (Medium) — Fix RenderFlex overflow.** Reproduce a Column overflow; solve with `Expanded` / `Flexible` / `SingleChildScrollView`." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 3 (Medium) — Read constraints in build.** Use `LayoutBuilder(builder: (context, constraints) => ...)` — a widget that gives you access to `BoxConstraints` at that point in the tree." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 4 (Hard) — Custom RenderBox.** Build the `PulseBox` sample above; extend it to draw a filled rounded rect." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 5 (Expert) — Custom multi-child RenderObject.** Subclass `RenderBox` with `ContainerRenderObjectMixin` to lay out two children side-by-side with a divider." },
    { "type": "theory", "markdown": "## Interview Q&A — with full answers you can quote" },
    { "type": "callout", "variant": "interview", "markdown": "**Q1: What is the render tree?**\n\n**A:** The render tree is a tree of `RenderObject` instances that own layout, paint, and hit-testing for the actual on-screen output. Each RenderObject implements `performLayout()` (compute size from parent constraints), `paint(ctx, offset)` (draw itself), and `hitTest(...)` (respond to input). RenderObjects are the expensive part — they carry sizes, offsets, layers, transforms. Flutter reuses them across rebuilds via the Element tree wherever the widget type and key match. The render tree is what actually turns into pixels — the widget/element trees are logic; the render tree is graphics." },
    { "type": "callout", "variant": "interview", "markdown": "**Q2: How does Flutter's layout algorithm work?**\n\n**A:** **Two passes, both in one frame:** \n\n**Down-pass (constraints):** the parent computes `BoxConstraints` for each child — 'you can be between min and max width/height' — and calls `child.layout(constraints, parentUsesSize: true)`.\n\n**Up-pass (sizes):** each child, having laid out its own children, sets its `size` to something within those constraints and returns.\n\nThen the parent positions each child by writing to `child.parentData.offset`. That's it — one downward walk for constraints, one upward walk for sizes, and a final positioning step. **Rules:** a child MUST honor its constraints; a parent must set `parentUsesSize: true` if it wants to read the child's size (otherwise Flutter can skip laying out the child when constraints alone determine parent size). This algorithm is called **'constraints go down, sizes go up, parent sets position'** and is drilled in every Flutter internals article." },
    { "type": "callout", "variant": "interview", "markdown": "**Q3: What's a `RepaintBoundary` and when do you use one?**\n\n**A:** `RepaintBoundary` inserts a boundary in the render tree that separates a subtree into its own compositing **layer**. When you use one, Flutter caches that subtree's paint output on the GPU; only if something INSIDE the boundary changes does the layer need to be re-recorded and re-uploaded. **When to use:** wrap any expensive subtree that changes at a different cadence from its neighbors. Classic case: a running animation next to a static list — without a `RepaintBoundary`, the entire ancestor layer re-records every animation frame. **Detect need:** DevTools 'Highlight Repaints' — layers turn different colors each time they re-record. If a static area is flashing, wrap it (or wrap the noisy animation) in a `RepaintBoundary`." },
    { "type": "callout", "variant": "interview", "markdown": "**Q4: What are `BoxConstraints`?**\n\n**A:** An immutable value class describing the range of sizes a child may return: `minWidth`, `maxWidth`, `minHeight`, `maxHeight`. Parents pass constraints to children during layout. Common constraint patterns: **`tight`** — min == max, forcing an exact size (e.g., `SizedBox`). **`loose`** — min=0, max=some value (e.g., `Center`, `Align`). **`unbounded`** — max = `double.infinity` (e.g., a `Row` inside a `SingleChildScrollView` gives unbounded horizontal). 'RenderFlex overflowed' happens when a `Row`/`Column`'s children take more space than the given constraints allow. 'Unbounded constraints' errors happen when a widget expects a finite max but got infinity. **Rule:** every layout bug is a constraint-chain bug — trace the constraints from root down." },
    { "type": "callout", "variant": "interview", "markdown": "**Q5: `RenderBox` vs `RenderSliver` — what's the difference?**\n\n**A:** **`RenderBox`** — the 2D-cartesian rendering model most of Flutter uses. Constraints are `BoxConstraints` (width, height ranges); the RenderObject returns a `Size` (width, height). Most widgets you use are `RenderBox`-backed. **`RenderSliver`** — the scrollable-axis model. Constraints are `SliverConstraints` (viewport extent, scroll offset, precede-scroll-extent, cross-axis extent, etc.); the sliver returns a `SliverGeometry` (its scroll extent, paint extent, cache extent, etc.). Slivers exist because scrolling is inherently one-dimensional and viewport-relative — you need to know how much is currently visible, how much of the sliver has been scrolled past, whether more can be built lazily. **When to use:** `SliverList`, `SliverGrid`, `SliverAppBar` inside a `CustomScrollView` for advanced scroll effects. Otherwise, regular boxes." },
    { "type": "callout", "variant": "interview", "markdown": "**Q6: When would you build a custom `RenderObject`?**\n\n**A:** **Rarely.** 99% of custom UIs can be built with `CustomPaint`, `Stack`+`Positioned`, `LayoutBuilder`, `Wrap`, or compositions of standard widgets. Real cases for a custom `RenderObject`: (1) you need a truly novel layout algorithm (a physics-based cloud of children, an interactive graph editor). (2) You're building a design-system primitive shared across many screens where a `CustomPaint` would be too slow. (3) You need direct control over hit-testing beyond `GestureDetector`. Even then, `SingleChildRenderObjectWidget` or `MultiChildRenderObjectWidget` scaffolding is usually the right base class. Always try `CustomPaint` first." },
    { "type": "callout", "variant": "interview", "markdown": "**Q7: What is `markNeedsPaint` vs `markNeedsLayout`?**\n\n**A:** Two low-level invalidation signals on `RenderObject`. **`markNeedsPaint()`** — tells Flutter 'my visuals changed but my SIZE didn't; re-record my layer next frame'. Called from paint-affecting property setters (color, decoration). Cheap — only paint runs. **`markNeedsLayout()`** — 'my SIZE may have changed; re-run layout for me and possibly my ancestors'. Called from size-affecting setters (width, padding). Expensive — layout is a full down/up pass. **Golden rule for custom RenderObjects:** every property setter must call the correct one. Miss it and the frame won't reflect your change. Both are automatically called on `set size` and framework-managed properties, so you only worry about custom fields." },
    { "type": "theory", "markdown": "## Real-world usage\n\n- **Every Flutter frame** — the render tree is walked twice (layout down, layout up), then painted.\n- **`Row`, `Column`, `Flex`** — `RenderFlex` with flex-based constraint distribution.\n- **`ListView`, `GridView`** — `RenderSliverList` / `RenderSliverGrid` for lazy scrolling.\n- **`CustomPaint`** — wraps a `RenderCustomPaint` you can control.\n- **`Transform`, `Opacity`, `ClipRect`** — each backed by a specialized RenderObject.\n- **DevTools 'Highlight Repaints'** — visualizes render-tree paint activity per frame." },
    { "type": "theory", "markdown": "## Advanced notes\n\n- **`RenderObject.attach(PipelineOwner)`** — attaches the RenderObject to a pipeline owner (usually one per window).\n- **`SchedulerBinding.instance.scheduleFrame`** — schedules a frame for layout+paint.\n- **`Layer` tree** — compositing layers built during paint; `PictureLayer`, `ClipRectLayer`, `TransformLayer`, `OpacityLayer`.\n- **`Compositing` phase** — after paint, Flutter's compositor uploads the layer tree to the GPU for rasterization.\n- **`RenderProxyBox`** — a common base class for RenderObjects that just wrap a child (e.g., `Padding`, `Opacity`).\n- **`Diagnosticable`** — RenderObjects support inspection via `debugFillProperties`.\n- **`RenderObject.debugDoingPaint`** — internal flag; use for assertions.\n- **`FlutterPreCompiled` shader Impeller integration** — modern Flutter paints via Impeller shaders." }
  ],
  "flashcards": [
    { "front": "Render tree job?", "back": "Layout + paint + hit-test the actual on-screen output." },
    { "front": "Layout algorithm?", "back": "Constraints go DOWN, sizes go UP, parent sets position." },
    { "front": "RenderBox vs RenderSliver?", "back": "Box = 2D cartesian (width/height); Sliver = scrollable-axis (viewport-aware)." },
    { "front": "RepaintBoundary?", "back": "Isolates a subtree's compositing layer so repaints don't propagate to neighbors." },
    { "front": "markNeedsPaint vs markNeedsLayout?", "back": "Paint = visuals only. Layout = size may change (more expensive)." },
    { "front": "Common overflow fix?", "back": "Wrap child in Expanded/Flexible/SingleChildScrollView — trace constraints from root." }
  ],
  "quiz": [
    { "q": "Constraints flow…", "options": ["Up only", "Down from parent to child", "Sideways", "Anywhere"], "answer": 1, "explain": "Sizes flow up in the second pass." },
    { "q": "RepaintBoundary isolates…", "options": ["Widgets", "A compositing layer for its subtree", "Elements", "Nothing"], "answer": 1, "explain": "Layer boundary." },
    { "q": "RenderSliver is for…", "options": ["2D UI", "Scrollable-axis layout in a viewport", "Animations", "Text only"], "answer": 1, "explain": "Viewport-aware." },
    { "q": "'RenderFlex overflowed' fix?", "options": ["Ignore it", "Wrap child in Expanded/Flexible/SingleChildScrollView", "Add more RAM", "Change SDK"], "answer": 1, "explain": "Constraint mismatch." },
    { "q": "Setting a color property should call…", "options": ["markNeedsBuild", "markNeedsPaint", "markNeedsLayout", "notifyListeners"], "answer": 1, "explain": "Visuals only, no layout change." }
  ],
  "challenges": [
    { "level": "Easy", "text": "Reproduce a RenderFlex overflow; fix with Expanded." },
    { "level": "Easy", "text": "Wrap a running animation in RepaintBoundary; verify with Highlight Repaints." },
    { "level": "Medium", "text": "Use LayoutBuilder to read BoxConstraints; branch layout for width < 600." },
    { "level": "Medium", "text": "Write a custom RenderBox (PulseBox) matching the sample." },
    { "level": "Hard", "text": "Extend PulseBox to draw a filled rounded rect and shadow." },
    { "level": "Hard", "text": "Subclass RenderBox with ContainerRenderObjectMixin for two children side-by-side." },
    { "level": "Expert", "text": "Profile a jank case with DevTools GPU frame graph; identify the offending layer/RenderObject." }
  ],
  "interviewQuestions": [
    "What is the render tree?",
    "How does Flutter's layout algorithm work?",
    "What's a RepaintBoundary and when do you use one?",
    "What are BoxConstraints?",
    "RenderBox vs RenderSliver — what's the difference?",
    "When would you build a custom RenderObject?",
    "What is markNeedsPaint vs markNeedsLayout?"
  ],
  "references": [
    { "label": "RenderObject — Flutter API", "url": "https://api.flutter.dev/flutter/rendering/RenderObject-class.html" },
    { "label": "Flutter's rendering pipeline (video)", "url": "https://www.youtube.com/watch?v=UUfXWzp0-DU" }
  ]
}
