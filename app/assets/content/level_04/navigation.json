{
  "id": "flutter.navigation",
  "levelId": 4,
  "title": "Navigation & Routing",
  "xp": 45,
  "estimatedMinutes": 35,
  "objectives": [
    "Understand navigation via a real-life analogy",
    "Distinguish Navigator 1.0 (imperative) vs 2.0 (declarative)",
    "Use go_router for typed routes, nested nav, deep links",
    "Pass and receive data between screens correctly",
    "Answer navigation interview questions confidently"
  ],
  "sections": [
    { "type": "callout", "variant": "analogy", "markdown": "**Think of navigation as a stack of paper business cards on your desk.**\n\n- Each screen is a **card** placed on top of the pile. You navigate to Detail? A new card lands on the stack. You press Back? The top card is thrown away. Simple.\n- **Navigator 1.0 (imperative)** was 'you manually drop each card on the pile': `Navigator.push(context, MaterialPageRoute(builder: (_) => DetailPage()))`. Works fine — but for **deep-linked web URLs** (like `myapp.com/user/42/orders`), you'd have to imperatively push every card in the right order every time. Fragile.\n- **Navigator 2.0 (declarative)** flips it: 'I'll TELL YOU what the stack should look like, YOU figure out how to get there'. Given `currentUrl == '/user/42/orders'`, the router determines: 'pile should be [Home, UserProfile(42), Orders]' and rebuilds the stack to match. That's what makes URL routing, deep links, and browser back/forward buttons work correctly on the web.\n- **`go_router`** is the community-blessed wrapper that makes Navigator 2.0 pleasant: typed routes, path parameters, redirect guards, nested navigators. Almost every modern Flutter app uses it.\n\n**Why this matters:** Smart Consumer and every production Flutter app will grow deep links, auth gates, and nested tab-bars. Understanding the model is the difference between fighting the framework and shipping." },
    { "type": "callout", "variant": "definition", "markdown": "**Flutter navigation** moves between screens ('routes') via the `Navigator` widget's stack. **Navigator 1.0** is imperative: `push`/`pop` calls mutate the stack directly. **Navigator 2.0** is declarative: you provide a `pages: []` list derived from app state, and Flutter builds the stack to match. Third-party wrappers (**`go_router`**, `auto_route`, `beamer`) give it typed sugar. All three modes coexist — you can start imperative and adopt go_router as complexity grows." },
    { "type": "theory", "markdown": "## Imperative — Navigator 1.0 (fine for simple apps)" },
    { "type": "code", "language": "dart", "code": "// Push — add a route on top of the stack\nfinal result = await Navigator.push(\n  context,\n  MaterialPageRoute(builder: (_) => const DetailPage(id: 42)),\n);\nif (result != null) print('detail returned: $result');\n\n// Pop — remove the top route; optionally return a value\nNavigator.pop(context, 'saved');   // in DetailPage's onPressed\n\n// Named routes — declare in MaterialApp.routes, navigate by name\nMaterialApp(routes: {\n  '/':       (_) => const HomePage(),\n  '/detail': (_) => const DetailPage(id: 0),   // args come via ModalRoute.of(context).settings.arguments\n});\nNavigator.pushNamed(context, '/detail', arguments: 42);\n\n// Replace — swap the top route\nNavigator.pushReplacement(context, MaterialPageRoute(builder: (_) => const HomePage()));\n\n// PopUntil — pop back to a specific point\nNavigator.popUntil(context, ModalRoute.withName('/'));" },
    { "type": "theory", "markdown": "## Declarative — go_router (the modern default)" },
    { "type": "code", "language": "dart", "code": "// Setup once — declare every screen and its URL up front\nfinal _router = GoRouter(\n  initialLocation: '/',\n  routes: [\n    GoRoute(path: '/', builder: (context, state) => const HomePage()),\n    GoRoute(\n      path: '/user/:id',                              // path param\n      builder: (context, state) => UserPage(\n        id: state.pathParameters['id']!,\n      ),\n      routes: [\n        // Nested — /user/42/orders\n        GoRoute(\n          path: 'orders',\n          builder: (context, state) => const OrdersPage(),\n        ),\n      ],\n    ),\n    GoRoute(\n      path: '/login',\n      builder: (context, state) => const LoginPage(),\n    ),\n  ],\n  // Auth guard — global redirect\n  redirect: (context, state) {\n    final loggedIn = AuthState.isLoggedIn;\n    final onLogin = state.matchedLocation == '/login';\n    if (!loggedIn && !onLogin) return '/login';\n    if (loggedIn && onLogin) return '/';\n    return null;   // no redirect\n  },\n);\n\n// Wire the router into MaterialApp\nMaterialApp.router(routerConfig: _router);\n\n// Navigate — type-safe, URL-aware\ncontext.go('/user/42');           // replace the current location\ncontext.push('/user/42/orders');  // push on top\ncontext.pop();                    // back\n\n// Deep links Just Work: opening 'myapp://user/42/orders' triggers the\n// same tree the router builds when the user navigates that path in-app." },
    { "type": "theory", "markdown": "## Sample program — a small app with 3 screens, deep link, and guard" },
    { "type": "code", "language": "dart", "code": "// pubspec: go_router: ^14.6.2\nimport 'package:flutter/material.dart';\nimport 'package:go_router/go_router.dart';\n\nclass Auth { static bool loggedIn = false; }\n\nfinal _router = GoRouter(\n  routes: [\n    GoRoute(path: '/', builder: (_, __) => const _Home()),\n    GoRoute(\n      path: '/product/:id',\n      builder: (context, state) => _Detail(id: state.pathParameters['id']!),\n    ),\n    GoRoute(path: '/login', builder: (_, __) => const _Login()),\n  ],\n  redirect: (context, state) {\n    final protecting = state.matchedLocation.startsWith('/product');\n    if (protecting && !Auth.loggedIn) return '/login';\n    return null;\n  },\n);\n\nvoid main() => runApp(MaterialApp.router(routerConfig: _router));\n\nclass _Home extends StatelessWidget {\n  const _Home();\n  @override\n  Widget build(BuildContext c) => Scaffold(\n    appBar: AppBar(title: const Text('Home')),\n    body: Center(child: FilledButton(\n      onPressed: () => c.push('/product/42'),\n      child: const Text('Open product 42'),\n    )),\n  );\n}\n\nclass _Detail extends StatelessWidget {\n  const _Detail({required this.id});\n  final String id;\n  @override\n  Widget build(BuildContext c) => Scaffold(\n    appBar: AppBar(title: Text('Product $id')),\n    body: Center(child: Text('details for $id')),\n  );\n}\n\nclass _Login extends StatelessWidget {\n  const _Login();\n  @override\n  Widget build(BuildContext c) => Scaffold(\n    appBar: AppBar(title: const Text('Login')),\n    body: Center(child: FilledButton(\n      onPressed: () { Auth.loggedIn = true; c.go('/'); },\n      child: const Text('Log in'),\n    )),\n  );\n}\n\n// Try:\n//   * Tap 'Open product 42' — redirected to /login.\n//   * Log in — bounced back to /.\n//   * Tap 'Open product 42' again — now the detail page shows.\n//   * `adb shell am start -a android.intent.action.VIEW -d 'myapp://product/42'`\n//     with the intent-filter set up → app opens directly on the product page." },
    { "type": "theory", "markdown": "## Common beginner mistakes" },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 1: Passing whole objects through routes.** Restoration and deep links break because the object may not exist yet on cold start. Pass **IDs** and refetch." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 2: Using context after `await Navigator.push(...)` without guarding.** Same async-gap bug as elsewhere. `if (!context.mounted) return;` or capture the Navigator before the await." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 3: Mixing imperative + declarative haphazardly.** Choose one style per module. Mixing means the back stack is derived from two sources of truth." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 4: No auth guard at the router level.** Doing auth-check inside each screen leads to flicker + duplicated code. Do it in the router's `redirect` callback." },
    { "type": "callout", "variant": "mistake", "markdown": "**Mistake 5: Nested Scaffolds with shared bottom nav done wrong.** Use `ShellRoute` (go_router) or a `Navigator` per tab to give each tab its own back stack." },
    { "type": "theory", "markdown": "## Best practices" },
    { "type": "callout", "variant": "best", "markdown": "**1. Default to `go_router` for anything non-trivial.** Type-safe, URL-first, deep-link ready." },
    { "type": "callout", "variant": "best", "markdown": "**2. Pass IDs through routes, not objects.** Refetch or read from cache/provider inside the destination." },
    { "type": "callout", "variant": "best", "markdown": "**3. Centralize auth in `redirect`.** One source of truth for guarded areas." },
    { "type": "callout", "variant": "best", "markdown": "**4. Use `ShellRoute` for persistent shells** (bottom nav, side bar). Each tab keeps its own back stack." },
    { "type": "callout", "variant": "best", "markdown": "**5. Test deep links with `adb shell am start`.** Every route should be reachable from a URL." },
    { "type": "callout", "variant": "best", "markdown": "**6. Enable web URL sync.** `MaterialApp.router` reflects the current location in the browser address bar automatically — big UX win on web." },
    { "type": "theory", "markdown": "## Practice programs" },
    { "type": "callout", "variant": "note", "markdown": "**Practice 1 (Easy) — Push + pop with a return value.** Home → picker → returns selected id → home displays it." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 2 (Medium) — go_router with a path param.** `/product/:id` builds a page that reads `state.pathParameters['id']`." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 3 (Medium) — Auth redirect.** `redirect` sends unauthenticated users to `/login`, back to `/` after login." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 4 (Hard) — ShellRoute + bottom nav.** 3 tabs, each with its own nested routes + independent back stack." },
    { "type": "callout", "variant": "note", "markdown": "**Practice 5 (Expert) — Deep link chain.** `myapp://user/42/orders` opens Home → UserProfile(42) → Orders — full back stack restored." },
    { "type": "theory", "markdown": "## Interview Q&A — with full answers you can quote" },
    { "type": "callout", "variant": "interview", "markdown": "**Q1: Imperative vs declarative navigation — differences and when to use each?**\n\n**A:** **Imperative (Navigator 1.0)** — you manually `push` and `pop` routes onto a stack in response to events. Simple mental model, easy to start. **Best for:** single-Activity apps with no URL/deep-link requirements. **Declarative (Navigator 2.0)** — you describe the *desired stack* as a function of app state; Flutter derives push/pop calls to match. Enables URL-driven navigation, browser back/forward, deep links, and state restoration. **Best for:** anything with web, deep links, or complex nested navigation. **Rule:** start imperative for prototypes; adopt `go_router` when you need URLs, deep links, guarded areas, or nested navigators. Modern production Flutter apps default to `go_router` on day one." },
    { "type": "callout", "variant": "interview", "markdown": "**Q2: How do you pass data between routes?**\n\n**A:** Three ways, in order of preference: **(1) Constructor / route builder** — safest and typed. `MaterialPageRoute(builder: (_) => DetailPage(id: 42))` or `GoRoute(builder: (c, s) => Detail(id: s.pathParameters['id']!))`. **(2) Path/query parameters (go_router)** — `/product/42?ref=email` — restorable and deep-linkable. **(3) Route settings arguments (Navigator 1.0)** — `pushNamed('/detail', arguments: obj)` read via `ModalRoute.of(context).settings.arguments`. Untyped and error-prone. **Return data from a pushed route:** `final result = await Navigator.push(...)`; in the pushed page `Navigator.pop(context, value)`. **Rule:** pass **IDs**, not object graphs — cold starts and deep links won't have your live object." },
    { "type": "callout", "variant": "interview", "markdown": "**Q3: What is `go_router` and why use it?**\n\n**A:** `go_router` is the community-standard wrapper over Navigator 2.0. It gives you: **(1) declarative route table** — every URL and its builder listed in one place; **(2) typed path parameters** — `/user/:id` yields `state.pathParameters['id']`; **(3) nested routes** via `routes:` children; **(4) `redirect` callback** — perfect for auth gates; **(5) `ShellRoute`** — persistent shells like bottom-nav with independent back stacks; **(6) deep-link support** — an incoming URL becomes an in-app navigation without any glue; **(7) works on web** — reflects location in the browser URL. Without go_router, you'd implement all of this against Navigator 2.0's raw API, which is powerful but verbose. Google increasingly recommends it — it's on the way to becoming the default." },
    { "type": "callout", "variant": "interview", "markdown": "**Q4: How do you implement an auth-protected route?**\n\n**A:** In go_router, use the top-level `redirect:` callback. It fires before every navigation and can return a new path to redirect to, or `null` to allow:\n```dart\nredirect: (context, state) {\n  final loggedIn = auth.isLoggedIn;\n  final atLogin = state.matchedLocation == '/login';\n  if (!loggedIn && !atLogin) return '/login';\n  if (loggedIn && atLogin) return '/';\n  return null;\n}\n```\nOne source of truth. Avoid ad-hoc auth checks in each screen — they cause flicker (screen briefly renders before redirect fires) and duplicated logic. Combine with a `refreshListenable` (a `ValueListenable<AuthState>`) so the router re-evaluates guards when the user logs in/out." },
    { "type": "callout", "variant": "interview", "markdown": "**Q5: What is a `ShellRoute` and when do you use one?**\n\n**A:** A `ShellRoute` (go_router) wraps a set of child routes in a **persistent shell widget** — a Scaffold with a `BottomNavigationBar`, a `NavigationRail`, a sidebar, etc. Its purpose: **each tab keeps its own back stack** while the shell (nav bar) stays mounted. Without ShellRoute, tapping a bottom-nav tab resets the tab's nested navigation. With it, users switch tabs freely and return to exactly where they were. **`StatefulShellRoute.indexedStack`** (a specialization) preserves not just the back stack but the WIDGET STATE per tab (scroll position, form data). Use it for multi-tab apps with independent per-tab navigation." },
    { "type": "callout", "variant": "interview", "markdown": "**Q6: How do deep links work in Flutter?**\n\n**A:** A deep link is a URL that opens a specific screen inside your app. Two components: **(1) OS setup** — the app declares URL patterns it handles via manifest intent filters (Android) or Universal Links (iOS). When the user taps a matching URL anywhere, the OS launches your app with that URL. **(2) Flutter side** — the router sees the incoming URL and matches it to a `GoRoute`, running the same builder as if the user had navigated in-app. `go_router` handles this automatically once you configure `MaterialApp.router(routerConfig: _router)`. Test with `adb shell am start -a android.intent.action.VIEW -d 'myapp://product/42'`. **Best practice:** use verified https deep links (App Links / Universal Links) — no chooser dialog, seamless UX." },
    { "type": "callout", "variant": "interview", "markdown": "**Q7: What is `Navigator.popUntil` and when do you use it?**\n\n**A:** `Navigator.popUntil(context, predicate)` pops routes off the stack until `predicate(route)` returns true. Common usage: `Navigator.popUntil(context, (r) => r.isFirst)` to pop back to the root, or `ModalRoute.withName('/home')` to pop to a specific named route. **Use cases:** logout (pop back to auth screen), 'Return to home' button in a deep nested flow, canceling a multi-step wizard. **In go_router:** `context.go('/home')` accomplishes the same by asserting a new location — the router pops or replaces to match. Prefer the go_router style in modern code — declarative intent maps more cleanly to your app's mental model." },
    { "type": "theory", "markdown": "## Real-world usage\n\n- **Smart Consumer, and every production Flutter app** uses `go_router` (or the deprecated Navigator 2.0 raw API).\n- **E-commerce apps** — `myapp://product/42` → opens directly on that product page.\n- **Chat apps** — `myapp://chat/abc123` → opens that conversation.\n- **Multi-tab apps (Twitter, Reddit)** — `ShellRoute` with independent back stacks per tab.\n- **Auth flows** — global `redirect:` guard.\n- **Flutter Web** — the router syncs to the browser URL; browser back/forward buttons work.\n- **Testing** — `WidgetTester.pumpWidget(MaterialApp.router(routerConfig: testRouter))` for router-driven test setups." },
    { "type": "theory", "markdown": "## Advanced notes\n\n- **`RouterDelegate` + `RouteInformationParser`** — the raw Navigator 2.0 APIs; `go_router` wraps them.\n- **`refreshListenable`** — a `Listenable` the router listens to; refires guards.\n- **`StatefulShellRoute.indexedStack`** — preserves per-tab widget state.\n- **`redirect` is called BEFORE the route is built** — no flicker on protected routes.\n- **Route transitions** — override `pageBuilder:` for custom transitions (fade, cupertino, custom hero).\n- **Restorable state** — `Navigator.restorablePush`, `RestorableRouteFuture` for state restoration.\n- **`GoRouterState`** — carries current path parameters, query parameters, extra data, name.\n- **`extra` parameter** — pass ephemeral non-URL data through go_router; won't survive deep links.\n- **`WidgetsBindingObserver.didPushRoute`** — hook for handling incoming URLs from the platform.\n- **`Uri.parse` + `Uri.pathSegments`** — parsing URLs manually if you must." }
  ],
  "flashcards": [
    { "front": "Navigator 1.0 vs 2.0?", "back": "1.0 = imperative push/pop. 2.0 = declarative pages list from state. Enables deep links + web URLs." },
    { "front": "Modern router of choice?", "back": "go_router — typed routes, guards, nested nav, ShellRoute, deep links." },
    { "front": "Pass data between routes?", "back": "Prefer IDs / path params, not whole objects. Deep links + cold start won't have live objects." },
    { "front": "Auth guard?", "back": "Global `redirect:` in the router; one source of truth." },
    { "front": "Persistent bottom nav w/ independent back stacks?", "back": "ShellRoute (or StatefulShellRoute.indexedStack for widget-state preservation)." },
    { "front": "Test deep link?", "back": "adb shell am start -a android.intent.action.VIEW -d 'myapp://path' or ios simulator xcrun." }
  ],
  "quiz": [
    { "q": "Modern router of choice?", "options": ["Navigator 1.0 only", "go_router", "Custom", "Hand-rolled RouterDelegate"], "answer": 1, "explain": "Standard in modern Flutter." },
    { "q": "Pass between routes — best?", "options": ["Whole object graph", "ID / path param (refetch on the other side)", "SharedPreferences", "GlobalKey"], "answer": 1, "explain": "Survives cold start + deep links." },
    { "q": "Auth guard goes in…", "options": ["Every screen", "Router-level redirect callback", "main()", "SharedPreferences"], "answer": 1, "explain": "One place, no flicker." },
    { "q": "ShellRoute is for…", "options": ["Nothing", "Persistent shells (bottom nav) with per-tab back stacks", "Web only", "Deprecated"], "answer": 1, "explain": "Modern multi-tab pattern." },
    { "q": "Deep link testing on Android?", "options": ["Restart", "adb shell am start -a … -d '<url>'", "flutter run", "Manual navigation"], "answer": 1, "explain": "Fires a VIEW intent." }
  ],
  "challenges": [
    { "level": "Easy", "text": "Push a picker; return a chosen id; display it on the home screen." },
    { "level": "Easy", "text": "Set up go_router with 3 routes and MaterialApp.router." },
    { "level": "Medium", "text": "`/product/:id` path param; read via state.pathParameters." },
    { "level": "Medium", "text": "Global auth `redirect:` — protected pages send to /login, back to previous after login." },
    { "level": "Hard", "text": "ShellRoute with 3 tabs, each with nested routes and independent back stacks." },
    { "level": "Hard", "text": "Add App Links / Universal Links so https deep links open your app without a chooser." },
    { "level": "Expert", "text": "Chain a deep-link path (`/user/42/orders`) that restores a 3-level back stack." }
  ],
  "interviewQuestions": [
    "Imperative vs declarative navigation — differences and when to use each?",
    "How do you pass data between routes?",
    "What is go_router and why use it?",
    "How do you implement an auth-protected route?",
    "What is a ShellRoute and when do you use one?",
    "How do deep links work in Flutter?",
    "What is Navigator.popUntil and when do you use it?"
  ],
  "references": [
    { "label": "Navigation overview — Flutter", "url": "https://docs.flutter.dev/ui/navigation" },
    { "label": "go_router", "url": "https://pub.dev/packages/go_router" }
  ]
}
